{
  "name": "code-yetu-soma",
  "version": "0.1.0",
  "private": true,
  "description": "Static Code Yetu Soma workshop app with local Playwright smoke hooks.",
  "scripts": {
    "serve:mock": "node scripts/mock-coach-server.js",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
